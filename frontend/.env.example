# Backend API base URL — used server-side by proxy routes only
BACKEND_URL=http://localhost:8000

# API key for backend authentication — server-side only, NEVER prefix with NEXT_PUBLIC_
API_KEY=your-api-key-here

# Public API URL exposed to browser — points to this Next.js app, not the backend directly
NEXT_PUBLIC_API_URL=http://localhost:3000
